# Copy this file to .env.local and fill in your backend's URL.
# .env.local is gitignored and never committed.

# Local development (your Express backend running locally):
VITE_API_BASE_URL=https://memorytree.onrender.com

# Production example (set this in Vercel's Environment Variables instead,
# not in a committed file):
# VITE_API_BASE_URL=https://your-backend.up.railway.app
